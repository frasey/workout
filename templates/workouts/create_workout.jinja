{% extends 'base.jinja' %} {% block content %}
    
<div>
    <ul>
    {% for exercise in exercises %}
        <li>{{exercise.name}}: {{exercise.sets}} sets {{exercise.reps}} reps
                <input name="add-exercise" type="submit" value="Add to workout"></li>
                {# checkbox instead of button? #}
    {% endfor %}
    </ul>
</div>

    </form>

    <h2>Didn't see it?</h2>

    <form action="/exercise/new" method="post">
        <label for="name">Name</label>
        <input type="text" name="name" id="name">

        <label for="sets">Sets</label>
        <input type="number" name="sets" id="sets">

        <label for="reps">Reps</label>
        <input type="number" name="reps" id="reps">
    
        <input type="submit" value="Add Exercise">
    </form>

{% endblock content %}